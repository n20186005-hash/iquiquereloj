---
import { getI18n } from '../i18n/ui';
import attraction from '../config/attraction';
const { messages } = getI18n(Astro.url);
const m = messages.mapSection;
const s = messages.sources ?? {};
const embedUrl = attraction.mapsEmbedSrc;
---

<section id="map" class="section-padding" style="background: var(--bg-secondary);">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="font-display text-3xl sm:text-4xl font-semibold mb-3 text-center" style="color: var(--text-primary);">{m.title}</h2>
    <p class="text-center text-base sm:text-lg mb-8" style="color: var(--text-secondary);">{m.subtitle}</p>

    <div class="map-container overflow-hidden border" style="border-color: var(--map-border);">
      <iframe
        src={embedUrl}
        class="w-full h-[320px] sm:h-[420px]"
        width="100%"
        height="450"
        style="border:0;"
        allowfullscreen
        loading="lazy"
        referrerpolicy="strict-origin-when-cross-origin"
        title={`${attraction.fullName} - ${attraction.city}, ${attraction.country}`}
      ></iframe>
    </div>

    <div class="text-center mt-8">
      <a href={attraction.mapsShareUrl} target="_blank" rel="noopener noreferrer" class="inline-flex w-full sm:w-auto justify-center px-8 py-3 rounded-full font-semibold text-white" style="background: var(--accent);">
        📍 {m.openMaps}
      </a>
    </div>

    <!-- 权威 .gob / .org 外链 -->
    <p class="mt-8 text-sm text-center leading-relaxed" style="color: var(--text-secondary);">
      {s.officialPortal}:
      <a href={attraction.govtTourismUrl} target="_blank" rel="noopener noreferrer" class="hover:opacity-70 underline" style="color: var(--accent);">
        {attraction.govtTourismName}
      </a>
    </p>
  </div>
</section>
